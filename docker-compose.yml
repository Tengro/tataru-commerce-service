services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - SCAN_DCS=Chaos
      - SCAN_INTERVAL_MINUTES=60

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
